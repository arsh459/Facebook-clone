/* const options = require("./cors/options")

"options" field can be used in cors for allowing the specific endpoints from where browser will allow the website to allow access to response.For Development purpose we will allow it from every endpoints.
 
app.use(cors(options))  */

/*
 */
